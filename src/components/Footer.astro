---
const currentYear = new Date().getFullYear();

const socialLinks = [
  { name: 'LinkedIn', href: 'https://www.linkedin.com/in/philippe-tremblay-36219485/' },
  { name: 'GitHub', href: 'https://github.com/philtremblay' },
  { name: 'Instagram', href: 'https://www.instagram.com/messoremclubcycliste/' },
];
---

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-info">
        <p class="footer-name">Philippe Tremblay</p>
        <p class="footer-role">Founder & Engineer</p>
      </div>

      <nav class="footer-links">
        {socialLinks.map((link) => (
          <a href={link.href} target="_blank" rel="noopener noreferrer" class="footer-link">
            {link.name}
          </a>
        ))}
      </nav>
    </div>

    <div class="footer-bottom">
      <p>&copy; {currentYear}</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--bg);
    border-top: 1px solid var(--border);
    padding: var(--space-20) 0 var(--space-12);
  }

  @media (min-width: 768px) {
    .footer {
      padding: var(--space-24) 0 var(--space-16);
    }
  }

  .footer-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
    margin-bottom: var(--space-12);
  }

  @media (min-width: 768px) {
    .footer-content {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-start;
    }
  }

  .footer-name {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: var(--space-1);
  }

  .footer-role {
    color: var(--text-light);
    margin: 0;
    font-size: 0.875rem;
  }

  .footer-links {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-6);
  }

  .footer-link {
    color: var(--text-light);
    font-size: 0.875rem;
    font-weight: 500;
    transition: color var(--transition);
  }

  .footer-link:hover {
    color: var(--text);
  }

  .footer-bottom {
    padding-top: var(--space-8);
    border-top: 1px solid var(--border);
  }

  .footer-bottom p {
    color: var(--text-subtle);
    font-size: 0.8125rem;
    margin: 0;
  }
</style>
