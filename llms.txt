# Philippe Tremblay - Portfolio Website

> Minimal portfolio website built with Astro.js in a Danish/Scandinavian minimalist design system.
> Static site showcasing software engineering expertise, projects, and professional background.

## Project Overview

A personal portfolio for Philippe Tremblay — software engineer, founder, and McGill Software Engineering graduate. Built with Astro.js for performance and SEO. Danish minimalist design: warm paper background, hairline rules, mono eyebrow labels, one muted clay accent, light/dark toggle, fully responsive.

Design lineage: structural cues (mono eyebrows, rule-separated rows, grain over flat colour, strict token system) adapted from hermes-agent.nousresearch.com; palette and typography are Danish minimalist rather than Hermes's electric blue.

Live site: https://philtremblay.github.io
Framework: Astro 5.15.8
Runtime: Deno 2.9.4 (also manages npm dependencies — no separate package manager)

## Architecture

This is an Astro.js static site using:
- File-based routing in /src/pages/
- Component-based UI in /src/components/
- Layout templates in /src/layouts/
- Global CSS with CSS variables for theming
- Zero JavaScript by default (Astro islands architecture)
- Minimal client-side JS only for theme toggle

### Key Technical Patterns

- **Static Generation**: All pages pre-rendered at build time to /dist/
- **Type Safety**: TypeScript configuration with strict mode
- **Theme System**: CSS variables + data-theme attribute + localStorage persistence, set by a blocking inline script in BaseLayout so there is no flash of the wrong palette
- **SEO**: Meta tags, Open Graph, canonical URLs in BaseLayout
- **Responsive Design**: Mobile-first CSS with breakpoints at 768px

## Key Files

### Configuration
- /deno.json - Deno tasks, npm import map (Astro 5.15.8), nodeModulesDir: auto
- /deno.lock - Committed lockfile pinning the full dependency graph
- /astro.config.mjs - Astro config for GitHub Pages (site, base path)
- /tsconfig.json - TypeScript strict mode configuration

### Layouts & Components
- /src/layouts/BaseLayout.astro - Main layout with SEO, meta tags, Header, Footer
  * Accepts title and description props
  * Includes global.css import
  * Sets up canonical URLs and Open Graph tags
- /src/components/Header.astro - Sticky masthead. Two-row grid on mobile (wordmark+toggle, then nav) collapsing to one row at 640px. No hamburger, no nav JS.
  * Theme toggle with localStorage persistence
  * Gradient logo text effect
  * Responsive navigation (hamburger menu on mobile)
- /src/components/Footer.astro - Site footer with social links and navigation
  * Social icons: LinkedIn, GitHub, Instagram
  * Copyright notice with dynamic year

### Pages
- /src/pages/index.astro - Homepage
  * Hero (eyebrow, serif headline, lede, two CTAs)
  * "What I do" — capabilities as hairline-separated rows
  * "Current work" — active work with status markers and tags
  * "Off the clock" — cycling; "Let's talk" — contact
- /src/pages/projects/index.astro - Current work, previous ventures, credentials register
- /src/pages/blog/index.astro - Notes index. Renders an empty state when `posts` is
  empty rather than padding with placeholder posts.

### Styles
- /src/styles/global.css - Design system: tokens, reset, type scale, shared primitives
  * Paper/ink palette (--paper*, --ink*), hairlines (--rule*), one accent (--clay*)
  * Light/dark theme color tokens
  * Spacing scale (--s-1 through --s-10), type scale (--step--1 through --step-4)
  * Three system font stacks: --font-display, --font-sans, --font-mono
  * Primitives: .shell, .section, .section-head, .rows/.row, .eyebrow, .index,
    .btn, .link, .tags, .status
  * Dark mode via [data-theme='dark'] attribute

### Static Assets
- /public/robots.txt - Search engine crawler instructions

### CI/CD
- /.github/workflows/deploy.yml - GitHub Actions workflow for automated deployment
  * Triggers on push to master branch (master is the default branch)
  * Uses Deno 2.9.4 (pinned) via denoland/setup-deno@v2, with dependency caching
  * Builds to /dist/ and deploys to GitHub Pages

## Design System (Danish Minimalist)

Never pure white, never pure black — everything is warm.

Paper (light): --paper #f2efe9, --paper-raised #f8f6f1
Ink (light): --ink #1c1a16, --ink-soft, --ink-muted, --ink-faint
Paper (dark): --paper #17150f — warm near-black, NOT blue-black
Hairlines: --rule / --rule-soft / --rule-strong (the layout is built from these)
Accent: --clay #9d4b33 — exactly one, for state and emphasis only, never decoration

Type — three faces, three jobs, zero webfont requests:
  --font-display (serif)     headings, wordmark, row titles
  --font-sans (grotesque)    body and UI
  --font-mono                eyebrows, indices, tags, buttons, status

Signature devices: .eyebrow (mono uppercase, 0.16em tracking), .index (01/02/03
numerals), .rows/.row (hairline-separated rows replacing cards), .section-head
(its rule doubles as the list's top edge), and a tiled SVG fractal-noise grain
on body::before. Border radius is 0 — this system does not round corners.

All colors defined as CSS variables in /src/styles/global.css for easy customization.

## Development Workflow

### Setup
```bash
# Requires Deno 2.9.4+ (https://deno.com/)
# No install step — Deno resolves and caches npm dependencies on first task run
```

### Commands
```bash
deno task dev      # Start dev server at http://localhost:4321
deno task build    # Build static site to /dist/
deno task preview  # Preview production build locally
deno task check    # Type-check the Astro project
```

### Project Structure
```
/
├── .github/workflows/    # GitHub Actions CI/CD
├── public/              # Static assets (robots.txt)
├── src/
│   ├── components/      # Reusable UI components
│   ├── layouts/         # Page layout templates
│   ├── pages/           # File-based routing (index, projects, blog)
│   ├── styles/          # Global CSS with theme variables
│   └── env.d.ts        # TypeScript environment definitions
├── astro.config.mjs    # Astro configuration
├── deno.json           # Deno tasks and npm import map
├── deno.lock           # Committed dependency lockfile
└── tsconfig.json       # TypeScript configuration
```

## Customization Guide

### Personal Information
- Hero copy and the `capabilities` / `work` arrays are in the frontmatter of /src/pages/index.astro
- Projects page data lives in the `current` / `previously` / `credentials` arrays in /src/pages/projects/index.astro
- Social links are in the `links` array in /src/components/Footer.astro
- Default title and meta description are in /src/layouts/BaseLayout.astro frontmatter

### Theme Colors
- Edit the token blocks at the top of /src/styles/global.css (:root and [data-theme='dark'])
- Change accent color by modifying --accent and --accent-hover variables
- Dark mode colors controlled by [data-theme='dark'] selectors

### Navigation
- Edit the `navItems` array in /src/components/Header.astro
- Update the `links` array in /src/components/Footer.astro

### Adding Blog Posts
- Create markdown files in /src/content/blog/ (structure prepared)
- Update /src/pages/blog/index.astro to list posts
- Consider adding Astro content collections for type-safe blog posts

## Deployment

Automated via GitHub Actions on push to master branch:
1. Workflow builds site with `deno task build`
2. Uploads /dist/ folder as GitHub Pages artifact
3. Deploys to https://philtremblay.github.io

Note: GitHub Pages must be set to "GitHub Actions" as the build source (not "Deploy from a branch") for this workflow to publish.

Manual deployment: Build with `deno task build` and deploy /dist/ folder to any static host (Vercel, Netlify, Cloudflare Pages, etc.)

## Recent Changes

- 2026-08-03: Danish minimalist redesign — new token system, mono eyebrows, rule-separated
  rows, grain texture; removed six fabricated placeholder projects and all emoji
- 2026-08-03: Upgraded to Deno 2.9.4 and pinned the version in CI; committed deno.lock
- 2025-11-16: Switched from Node.js/pnpm to the Deno runtime (deno.json replaces package.json)
- Complete redesign from vanilla HTML/CSS/JS to Astro.js framework
- Implemented light/dark mode via CSS variables
- Added automated GitHub Pages deployment via GitHub Actions

## Notes for AI Agents

- This is a static site - no backend, API routes, or database
- All content is hardcoded in .astro page frontmatter (not using CMS or content collections yet)
- Content is real. An earlier version of /projects listed six invented projects; they were
  removed rather than restyled. Do not add placeholder work — leave sections short instead.
- Theme is set pre-paint by an inline script in BaseLayout.astro; Header.astro only handles the click
- CSS is scoped by component except for global.css
- Build output is excluded from git (see .gitignore for dist/, node_modules/); deno.lock IS committed
- Site is optimized for perfect Lighthouse scores (minimal CSS, zero JS overhead)
- Personal information (name, LinkedIn, GitHub) is scattered across multiple files
- Blog structure exists but is not populated with actual posts yet
